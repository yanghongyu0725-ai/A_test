# A_test

这是一个用于演示与后续开发的初始化仓库。

## 目录结构

- `src/`：业务源码目录
- `tests/`：测试代码目录
- `docs/`：文档目录
- `scripts/`：辅助脚本目录

## 快速开始

当前仓库仅完成基础脚手架初始化。你可以在 `src/` 中开始编写代码，在 `tests/` 中补充测试。

## 新人建议

1. 先阅读 `README.md` 和 `docs/onboarding.md`
2. 在 `src/` 添加第一个最小可运行模块
3. 在 `tests/` 增加对应单元测试
